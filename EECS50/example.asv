img = imread("coins.");
gray = rgb2gray(img);
imshow(gray);


noisy = imnoise(gray, 'gaussian', 0.01);
imshow(noisy);

h = (1/3) * [1 1 1];   % 3-point moving average
filtered = conv2(noisy, h, 'same');
imshow(uint8(filtered));

freqz(h, 1);